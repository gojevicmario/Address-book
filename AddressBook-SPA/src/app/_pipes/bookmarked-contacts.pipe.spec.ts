/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BookmarkedContactsPipe } from './bookmarked-contacts.pipe';

describe('Pipe: BookmarkedContactse', () => {
  it('create an instance', () => {
    let pipe = new BookmarkedContactsPipe();
    expect(pipe).toBeTruthy();
  });
});
